<form action="/landmarks" method="post">

  <h1>Landmark:<h1><input type = "text" id="landmark_name" name = "landmark[name]"><br>
  <!-- <h2>New figure:<h2><input type = "text" id="new_figure" name = "figure[name]"><br>
  <h2>New title:<h2><input type = 'text' id="new_title" name = "title[name]"><br>
  <h3>Landmarks</h3>
  <% @figures.each do |figure| %>
    <label><%= figure.name %></label>
    <input type = 'checkbox' name = 'landmark[figure_ids][]' id="figure_<%=figure.id%>" value = '<%= figure.id%>'>
    <br>
  <%end%>
  <h3>Titles</h3>
  <% @titles.each do |title| %>
  <label><%= title.name %></label>
  <input type = 'checkbox' name = 'landmark[title_ids][]' id="title_<%=title.id%>" value = '<%= title.id%>'>
  <br>
  <%end%> -->
  <h2>Year_completed</h2><input type = "text" id="landmark_year_completed" name = "landmark[year_completed]"><br>
  <input type = "submit" value = "Create New Landmark">

</form>
